services:
  trino:
    build: .
    ports:
      - "8080:8080"
    environment:
      - CYPHERA_POLICY_FILE=/etc/cyphera/cyphera.json
    volumes:
      - ./config/cyphera.json:/etc/cyphera/cyphera.json:ro
